requests>=2.31
python-dotenv>=1.0
gpiozero>=2.0
opencv-python-headless>=4.9
numpy>=1.26
Pillow>=10.0

# picamera2 (and its libcamera bindings, incl. the Python `controls`
# module) are NOT installed via pip here — on Raspberry Pi OS they come
# from apt (python3-picamera2 python3-libcamera). See README.md
# "Raspberry Pi setup".
